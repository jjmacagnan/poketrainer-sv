import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Política de Privacidade · PokéTrainer SV",
  description: "Como o PokéTrainer SV coleta e utiliza dados dos visitantes.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
